import React from "react";

const MobileSettingsPageTest = ({ onBack }) => {
  return (
    <div>
      <h1>Test Settings Page</h1>
      <button onClick={onBack}>Back</button>
    </div>
  );
};

export default MobileSettingsPageTest;
